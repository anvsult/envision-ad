name: Jacoco Code Coverage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Run Gradle build
        run: |
          cd webservice
          doppler run -- project envision-ad-backend --config prd --token ${DOPPLER_BACKEND_TOKEN} -- \
          ./gradlew clean build --no-daemon

      - name: Upload JaCoCo HTML report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-html
          path: webservice/build/reports/jacoco/test/html